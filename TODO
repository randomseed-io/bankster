- document user's file (META-INF)
- document data structures (config.edn, registry)
- documentation review
- specs

- ability to update currency without deleting country data and localized properties

- check if updating numeric id updates data in cur-code->cur
  - should, since it removes the currency first

- XXX technical currency should be stored somewhere, not taken from a registry
